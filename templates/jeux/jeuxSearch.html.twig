{{ form_start(form,{'attr': {'class': 'form-sample', 'novalidate': 'novalidate'}} ) }}
<div class="leave-comment-wrap">
   <div class="row">
    <div class="col-md-6">
        <div class="item">
            <label>
                <span>{{ form_widget(form, {'attr': {'class': 'form-sample', 'novalidate': 'novalidate'}})}}</span>
                <span><button class="btn btn-primary mr-2">{{ button_label|default('Save') }}</button></span>
            </label>
            </div>	
        </div>
    </div> 
{{ form_end(form) }}