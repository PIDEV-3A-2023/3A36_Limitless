<form method="post" action="{{ path('app_sponsorback_delete', {'id': sponsor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sponsor.id) }}">
    <button class="badge badge-danger" class="btn">Delete</button>
</form>
