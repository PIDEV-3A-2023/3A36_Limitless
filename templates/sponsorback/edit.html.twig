{% extends 'back.html.twig' %}

{% block title %}Edit Sponsor{% endblock %}

{% block body %}
    <h1>Edit Sponsor</h1>

    {{ include('sponsorback/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_sponsorback_index') }}">back to list</a>

    {{ include('sponsorback/_delete_form.html.twig') }}
{% endblock %}
