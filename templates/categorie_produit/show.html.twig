{% extends 'back.html.twig' %}

{% block title %}CategorieProduit{% endblock %}

{% block body %}
    <h1>CategorieProduit</h1>

    <table class="table">
        <tbody>
        {% if categorie_produit !=null%}
            <tr>
                <th>Id</th>
                <td>{{ categorie_produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie_produit.nom }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_categorie_showCategorieProduit') }}"><button class="btn btn-warning">{{ button_label|default('Back') }}</button></a>

    

    {{ include('categorie_produit/_delete_form.html.twig') }}
     {%endif%}
      <style>
table {
  width: 100%;
  border-collapse: collapse;
  font-family: Arial, sans-serif;
  margin: 20px auto;
}

tr {
  border-bottom: 1px solid #dddddd;
}

td {
  padding: 5px;
  line-height: 1.2; /* la valeur a été réduite à 1.2 */
  text-align: left;
}
tr:nth-child(even) {
  background-color: #f2f2f2;
}

<style>
{% endblock %}
